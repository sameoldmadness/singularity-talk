hooks:
  afterAllFileWrite:
    - prettier --write
generates:
  ./generated/types.ts:
    schema: ./lib/schema.ts
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
