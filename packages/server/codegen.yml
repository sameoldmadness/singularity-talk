hooks:
  afterAllFileWrite:
    - prettier --write
generates:
  ./generated/types.ts:
    schema: ./lib/schema.graphql
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
